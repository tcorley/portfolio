another stab at a portfolio site leveraging a modern stack

## Getting Started

The app leverages [Next.js](https://nextjs.org/). Just install the dependencies:

```bash
pnpm i
```

then run the development server:

```bash
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
