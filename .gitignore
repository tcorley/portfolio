.DS_Store
.env
/node_modules/
*.tsbuildinfo

# React Router
/.react-router/
/build/

# Cloudflare
.mf
.wrangler
.dev.vars*
worker-configuration.d.ts

# Resume tooling: the canonical source is public, while generated and
# application-specific material remains local. Deploy copies the PDF into
# public/resume.pdf so the site can serve https://rolan.dev/resume.pdf.
/resume/.venv/
/resume/build/
/resume/local-applications/
/resume/resume.selected.yaml
/resume/folio/__pycache__/
/public/resume.pdf


!.dev.vars.example
!.env.example
